# Autoresearch: UT 优化

## 目标
优化单元测试：保持覆盖率 90%+，提升性能和速度，用有意义的测试取代无意义的。

## 主要指标
- **primary metric**: 测试运行时间 (ms)
- **secondary metrics**: 覆盖率 (%)、测试数量

## 约束
- 覆盖率不能低于 90%
- 原子化提交，不 push
- 不能作弊（如跳过测试、降低覆盖率换速度）

## 优化方向
1. 减少重复的 setup/teardown
2. 合并冗余测试用例
3. 使用更快的断言方式
4. 减少不必要的 async/await
5. 优化慢测试（crypto 操作等）
6. 移除无意义的测试（如纯类型测试）

## 当前基线
- 运行 `bun test --coverage` 获取基线数据
