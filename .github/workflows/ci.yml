name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  quality:
    uses: nocoo/base-ci/.github/workflows/quality.yml@3d2e9e55fdd60aa756df454ded53ef6d06d6653f
    with:
      runtime-version: "1.4.2"
      install-policy: "trusted"
      coverage-path: "coverage"
      prepare-command: "bun run build"
      test-command: "bun run test:coverage"
      typecheck-command: "bun run typecheck"
      l2: true
      l2-command: "bun run test:l2:http"
    secrets: inherit

  browser-e2e:
    name: "L3 Browser E2E"
    needs: [quality]
    uses: nocoo/base-ci/.github/workflows/test-job.yml@3d2e9e55fdd60aa756df454ded53ef6d06d6653f
    with:
      runtime-version: "1.4.2"
      install-policy: "blocked"
      browser: "chromium"
      browser-working-directory: "apps/web"
      timeout-minutes: 25
      command: "bun run test:e2e:browser"
